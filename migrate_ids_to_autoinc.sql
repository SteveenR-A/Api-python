-- (ARCHIVO MARCADO COMO DEPRECATED)
-- Este archivo de migración fue dejado como referencia, pero la estructura ya
-- fue exportada a `db_schema.sql` y no es necesario ejecutar estas ALTERs.
-- Si deseas eliminar este archivo, hazlo manualmente o usa `git rm migrate_ids_to_autoinc.sql`.

-- Nota: conservar el archivo como histórico puede ser útil, por eso se dejó comentado.
